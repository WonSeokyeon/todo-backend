# 줄바꿈 정규화
# core.autocrlf 개인 설정과 무관하게 저장소 전체를 LF 로 통일한다.
* text=auto eol=lf

# Maven 래퍼: 유닉스용은 LF, 윈도우용은 CRLF 여야 실행된다.
/mvnw text eol=lf
*.cmd text eol=crlf
*.bat text eol=crlf
*.sh text eol=lf

# 바이너리
*.jar binary
*.png binary
*.jpg binary
*.ico binary
*.p12 binary
*.jks binary
